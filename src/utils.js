/**
 * Utility module across the project.
 * @module utils
 */

/**
 * Callback for returning string.
 *
 * @callback strCallback
 * @param err {Error} Created error while executing.
 * @param data {String} Returned string.
 */
